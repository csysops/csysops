TRANSPORT LAYER: TCP, UDP, SCTP
1. intro
UDP, datagram, unreliable
tcp, byte-stream, reliable
sctp, reliable, msg bound, multihoming, hol blocking,
service 
2. big-picture 16 
- tcp/ip suite; tcpdump; traceroute; IPv4 3; ipv6 3; tcp 5; udp 3; sctp 4; icmp; igmp; arp; rarp; icmp6; bpf/dlpi; RPC; dual-stack;
3. udp 6
- simple, unreliable 3, connectionless 2, message-based 2, unlike; packaging
4. tcp 7
- connection-oriented 3, reliability 5, timeout, sequencing 3, flow control 2, full-duplex 3, unlike udp 2
5. sctp

association-oriented
reliable-, 
sequencing-fragment message to chunks, label each chunk uniquely with tsn, 
tsn-first send first arrive, sid which stream, ssn-fragment 
flow control, 
full-duplex
multihoming 
assoc, meaning the association between 2 endpoints is a set of ips + port so provide many combination path, robustness
1 msg, 1 ssn
1 chunk, 1 tsn

chunk
  data tsn, sid, ssn, flag with 2 bit either ibem and 1 bit either unordered
  control
global order
which stream
message order per stream
  queue, same ssn array chunk sort by tsn and ssn+1 after and each forming 1 
  complete message in order
  avoid hol
flag 
redundant network connections meaning preconnect host interface with different path to net infra include ether and wifi and 4g

failover, autoly switch path when heartbeat and timeout fail

lobby is nested array stream,
ssn 1 arrive, put lobby, ssn 0 arrive, put receiver buffer then check if next_ssn is in lobby loop

prev same ssn chunk always comes before current chunk.
message-oriented, marking
sequencing, each write order
multi-streaming, isolated sequencing, avoid hol meaning msg B wait msg A in stream 1 doesn't block msg X in stream 2 to read, logic filter, one association with many lanes
lobby stores complete messages
each stream
struct sctp_stream_in {
    uint16_t next_ssn;
    queue lobby;   // out-of-order messages
}

structure of reasm
reasm_table[SSN=10] = {
    fragments = { TSN 500, 501, 502 },
    have_first = true/false,
    have_last = true/false
}


struct sctp_ulpq {
    reasm_queue;   // fragments waiting
    lobby_queue;   // full messages waiting for SSN
}

A collection of incomplete messages, each made of multiple fragments (TSNs)
“Store all fragments waiting” (implementation)
reasm_queue is linked list of heads, each head having metadata of what ssn, each chunk of same existed ssn will go link to the right head, message chain, each chain also keeping order by tsn

reasm_queue is a top-level list where each element represents one message, and each message internally is a linked list of fragments — effectively a list of lists.
so for each chain, each element in the chain will be the same ssn?
