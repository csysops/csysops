TRANSPORT LAYER: TCP, UDP, SCTP
1. intro
UDP, datagram, unreliable
tcp, byte-stream, reliable
sctp, reliable, msg bound, multihoming, hol blocking,
service 
2. big-picture
- tcp/ip suite; tcpdump; traceroute; IPv4 3; ipv6 3; tcp 5; udp 3; 
sctp
support multihoming so the connection of 2 endpoint is called association(ips,port<->ips,port),
it is connection-oriented connection must establish, meaning both should on the meeting room, meaning they ready to listen to the other,
full-deplex, record boundaries, use both ip in one version
- icmp; igmp; arp; rarp; icmp6; bpf/dlpi; RPC; dual-stack
3. 

