TRANSPORT LAYER: TCP, UDP, SCTP
1. intro
UDP, datagram, unreliable
tcp, byte-stream, reliable
sctp, reliable, msg bound, multihoming, hol blocking,
service 
2. big-picture 16 
- tcp/ip suite; tcpdump; traceroute; IPv4 3; ipv6 3; tcp 5; udp 3; sctp 4; icmp; igmp; arp; rarp; icmp6; bpf/dlpi; RPC; dual-stack;
3. udp 6
- simple, unreliable 3, connectionless 2, message-based 2, unlike; packaging
4. tcp 7
- connection-oriented 3, reliability 5, timeout, sequencing 3, flow control 2, full-duplex 3, unlike udp 2
5. sctp


stream is logic filter
message-oriented, one read to get the entire msg and its length
1 association = nhiều message stream
multistreaming, one association with many lanes and each one is isolated reliable msg-delivery, avoid hol blocking meaning msg B wait msg A in stream 1 doesn't block msg X in stream 2 to read

tcp 1 stream
redundant network connections, sử dụng các đường mạng đã có sẵn để gửi ngay khi lỗi
multihoming
connection is a state in each side, seq, buffer, c
tìm đường khác 
Connection = “đường logic”
Data = thứ chạy trên đó
one port, many association, dst port just differ src port
loopback interface luôn sống
association chứa các tập hợp
assign ip telling meaning of each interface
giữ nguyên port, khi message tới, chỉ cần src ip có nằm trong tập hợp
each sending path with multiple ip connect to one des
redundant network connections, preconnect multiple host interface with different path to des host in one association, when one path fail, switch another

connection, 4-tuple
one port, many asssociation, ip variable so cant use fixed 4-tuple for identity so vtag 
Association =
{
    src_port,
    dst_port,
    local_vtag,
    remote_vtag,
    local_IP_set = {IP1, IP2, ...},
    remote_IP_set = {IP3, IP4, ...}
}
