TRANSPORT LAYER: TCP, UDP, SCTP
1. intro
UDP, datagram, unreliable
tcp, byte-stream, reliable
sctp, reliable, msg bound, multihoming, hol blocking,
service 
2. big-picture
TCP/IP, much
tcpdump, direct link, bpf, dlpi, except
to dive in is to use socket, except
traceroute use both socket for IP va ICMP
IPv4, 1981, 32-bit, packet delivery, TCP, UDP, SCTP, ICMP và IGMP
IPv6, explosion, 1998, 128-bit, packet delivery, TCP, UDP, SCTP và ICMPv6.
tcp, connection-oriented, byte stream, fuck duplex, reliable, tcp socket -> stream socket, ack, timeout, retran, up
udp, connectionless, udp socket -> datagram socket, unreliable, up
SCTP, connection-oriented, full-duplex association, multihoming, bound, up both
ICMP, “ngôn ngữ nội bộ” của network, handle error, control infor with route
IGMP, multicasting, ipv4 option
arp,
