TRANSPORT LAYER: TCP, UDP, SCTP
1. intro
UDP, datagram, unreliable
tcp, byte-stream, reliable
sctp, reliable, msg bound, multihoming, hol blocking,
service 
2. big-picture 16 
- tcp/ip suite; tcpdump; traceroute; IPv4 3; ipv6 3; tcp 5; udp 3; sctp 4; icmp; igmp; arp; rarp; icmp6; bpf/dlpi; RPC; dual-stack;
3. udp 6
- simple, unreliable 3, connectionless 2, message-based 2, unlike; packaging
4. tcp 7
- connection-oriented 3, reliability 5, timeout, sequencing 3, flow control 2, full-duplex 3, unlike udp 2
5. sctp
- arsff
msg-oriented, preserve message soft bound so read until EOR
multihoming 
  assoc meaning the connection between 2 endpoints in each side is a set of ips + port so provide many combination path
  keep redundant network connections meaning preconnect host interface with different path to net infra include ether and wifi and 4g
  failover, autoly switch path when heartbeat and timeout fail so robustness

multi-streaming, 
  logic filter for each isolated sequencing, 
  avoid hol meaning msg B wait A in stream 1 doesn't block X in stream 2 to read
6. tcp con estab and ter
- 
